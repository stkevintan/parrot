{
  "name": "@stkevintan/parrot",
  "version": "0.1.4",
  "description": "convert yapi or swagger json to typescript code",
  "main": "dist/index",
  "scripts": {
    "build": "rm -rf dist && tsc --outDir dist && cp -rf src/template dist/"
  },
  "author": "Kevin Tan",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^12.11.5",
    "@types/nunjucks": "^3.1.2",
    "axios": "^0.19.0",
    "json-to-pretty-yaml": "^1.2.2",
    "nunjucks": "^3.2.0",
    "@types/swagger-schema-official": "^2.0.19"
  }
}
